const express = require('express'); const bodyParser = require('body-parser'); const axios = require('axios'); require('dotenv').config(); const app = express(); app.use(bodyParser.json()); app.get('/create-checkout', async (req,res)=>{ res.json({url:'https://vendors.paddle.com/checkout/sample'}); }); app.post('/webhook',(req,res)=>{ console.log('webhook',req.body); res.send('ok'); }); const PORT=process.env.PORT||8080; app.listen(PORT,()=>console.log('Paddle demo running',PORT));
